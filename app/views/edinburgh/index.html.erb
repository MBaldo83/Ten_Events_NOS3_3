
<h1> Edinburgh </h1>

<li><%= link_to "New Event", new_all_event_path %></li>

<h2> Ten Events </h2>

<%= render 'shared/index_events_table', :events_group => @ed_ten_events %>

<h2> All Ten Track's Events </h2>

<%= render 'shared/index_events_table', :events_group => @ed_all_events %>

<li><%= link_to "New Venue", new_venue_path %></li>

<h2> Saved venues </h2>

<%= render 'shared/index_venues_table', :venues_group => @ed_venues %>

<h2> ------------------------------ </h2.
<h2> All Events (for reference only)</h2>

<%= render 'shared/index_events_table', :events_group => @all_events %>




